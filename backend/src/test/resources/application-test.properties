# Test database — values overridden by CI environment variables
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/gamelibrary_test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:dev}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:devpassword}

spring.jpa.hibernate.ddl-auto=none
spring.flyway.locations=classpath:db/migration

logging.level.root=WARN
