# Test database — default uses docker-compose service name; CI overrides via env vars
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/gamelibrary_test}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:dev}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:devpassword}

spring.jpa.hibernate.ddl-auto=none
spring.flyway.locations=classpath:db/migration

logging.level.root=WARN
